{
  "id": "agent-first-data",
  "name": "Agent-First Data",
  "domain": "cmn.dev",
  "synopsis": "The field name is the schema \u2014 agents read latency_ms and know milliseconds, api_key_secret and know to redact, no external schema needed",
  "intent": [
    "Encode all semantic information in the field name itself so agents parse structured data without external schemas, documentation, or type systems \u2014 _ms/_s/_epoch_ms for time, _msats/_sats/_usd_cents for currency, _bytes for size, _secret for auto-redaction",
    "Define a JSONL output protocol where every line carries a code field (startup for config state, tool-defined for status/progress, ok/error for results) plus a trace object for execution context (duration, token counts, cost, data source) \u2014 same structure whether the transport is CLI stdout, REST API response, MCP tool result, or SSE stream",
    "Provide three output formats via --output json|yaml|plain: JSON is the canonical lossless format, YAML preserves structure with quoted strings and --- separators, plain applies suffix-driven human formatting (epoch_ms \u2192 RFC 3339, _bytes \u2192 human-readable, _secret \u2192 ***, _ms \u2265 1000 \u2192 seconds)"
  ],
  "changes": [
    "Add AFD-compliant structured logging module to all four languages (Rust/Go/Python/TypeScript), integrating with each language's native logging ecosystem (tracing, log/slog, logging, built-in) with span support for concurrent request disambiguation \u2014 Rust uses tracing spans, Go uses slog WithAttrs + context, Python uses contextvars, TypeScript uses AsyncLocalStorage",
    "Support all three output formats (JSON/Plain/YAML) in every logging module by routing through the library's own output_json/output_plain/output_yaml functions, so AFD suffix processing (key stripping, value formatting, secret redaction) applies to log fields identically to the core output API",
    "Update all documentation \u2014 four language READMEs with detailed logging API reference, root README with format comparison table, skill file with logging init table, span examples, CLI --output flag convention, and review checklist item for structured logging"
  ],
  "license": "MIT",
  "references": [
    {
      "uri": "cmn://cmn.dev/spore/blake3_8d507cb24143b5666a8d9d40a6b8c756ce8a34891a052e4a21858e99e2d384fe",
      "relation": "spawned_from"
    }
  ],
  "tree": {
    "algorithm": "blob_tree_blake3_nfc",
    "excludes": [
      ".git"
    ],
    "ignore_rules": [
      ".gitignore"
    ]
  }
}
